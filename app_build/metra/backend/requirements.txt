# --- Core API ---
fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic[email]==2.9.2
python-multipart==0.0.9

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1  # passlib 1.7.4 is incompatible with bcrypt>=4.1's API

# --- Database ---
sqlalchemy==2.0.35
# psycopg2-binary==2.9.9   # uncomment for Postgres upgrade path

# --- Compliance pipeline ---
Pillow==10.4.0

# --- OCR (only required when METRA_OCR_ENGINE=paddleocr) ---
# paddlepaddle==2.6.2
# paddleocr==2.8.1
# opencv-python-headless==4.10.0.84
# numpy==1.26.4

# --- Vector DB (Cycle 2: Ask METRA + seller resolution, not used yet this cycle) ---
# chromadb==0.5.5
# sentence-transformers==3.1.1
